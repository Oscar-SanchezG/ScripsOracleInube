Abrimos Docker Desktop
Abrimos la Terminal
Nos movemos a la ubicación del archivo docker-compose.yml
Estando en la ruta en la terminal ejecutamos el siguiente comando:
docker compose up -d
Esperamos a que haga la descarga de la imagén y se levante el contenedor
oracle # Password para SYS, SYSTEM, PDBADMIN